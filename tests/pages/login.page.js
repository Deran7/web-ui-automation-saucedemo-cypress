export const usernameField = '#user-name' // use elements id
export const passwordField = '[name="password"]' //use elements name
export const loginButton = '."submit-button' // use elements class
export const errorAlert = '[data-test="error"]' // use elements data test (custom elements by dev)